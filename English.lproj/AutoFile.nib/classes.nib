{
    IBClasses = (
        {
            ACTIONS = {done = id; }; 
            CLASS = BibFiler; 
            LANGUAGE = ObjC; 
            OUTLETS = {infoTextField = NSTextField; tv = NSTableView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = "java.lang.Object"; LANGUAGE = Java; }
    ); 
    IBVersion = 1; 
}