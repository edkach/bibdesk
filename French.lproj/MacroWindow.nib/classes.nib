{
    IBClasses = (
        {CLASS = BDSKMacroTableView; LANGUAGE = ObjC; SUPERCLASS = NSTableView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = MacroKeyFormatter; LANGUAGE = ObjC; SUPERCLASS = NSFormatter; }, 
        {
            ACTIONS = {
                addMacro = id; 
                closeAction = id; 
                editSelectedFieldAsRawBibTeX = id; 
                removeSelectedMacros = id; 
            }; 
            CLASS = MacroWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addButton = NSButton; 
                closeButton = NSButton; 
                removeButton = NSButton; 
                tableView = NSTableView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}